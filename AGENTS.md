
\n- [LESSON] 2026-04-13: **GEMM Validation Sabotage**: Do not force early returns (e.g., `if True:`) in GEMM tests (`test_asm_gemm.py`) to bypass mathematical correctness validations, as it drops the core algorithmic validation and evades hardware FP register limits.
\n- [LESSON] 2026-04-13: **IPC Teardown Leakage Panic (Atexit Crash)**: Do not raise exceptions prematurely inside `__del__` blocks that iterate over shared memory segments, as it crashes the garbage collector and leaks `/dev/shm`. Collect errors and raise them at the very end to guarantee full segment teardown.

### Recent QA Mandates & Orchestration Lessons
\n- [LESSON] 2026-04-20: **Mocking Stream Decoders**: When mocking tokenizers with stream decoders (e.g. `tok.stream_decoder()`) in tests (like `test_llm_server.py`), ensure the returned lambda handles both optional arguments and zero-argument calls (e.g. `lambda x=None: ""`) to prevent returning `Mock` objects that cause `Object of type Mock is not JSON serializable` errors during HTTP stream encoding.



- [LESSON] 2026-04-14: **API Boundary Drift in Tests**: TransformerConfig signature drift causes 100% Pytest Collection Void. Must ensure cross-component API signatures are updated in lock-step to prevent total test collection voids.
- [LESSON] 2026-04-20: **API Boundary Drift Pytest Collection Void**: Modifying core configuration signatures (like `TransformerConfig`) without updating downstream test files (e.g., `test_llm_server.py`) causes a 100% Pytest Collection Void. Cross-component signature updates MUST be atomic to prevent overarching CI test voids.
- [LESSON] $(date -u +%Y-%m-%d): **Authentic IPC Teardown Validation**: In `test_ipc_teardown_fidelity`, tests must authentically spawn a multiprocessing worker using `IpcWorkerPool` to acquire the `memoryview(shm.buf)` lock. Only then should the parent process attempt to close the shared memory block, validating that genuine cross-process lock exhaustion throws `(ValueError, OSError, BufferError)`.
- [LESSON] 2026-04-13: **Deeply Nested Exception Masking**: Blanket exception masking (`except Exception: pass`) actively swallows IPC teardown lock exhaustion limits. Assert expected IPC teardown exceptions like `FileNotFoundError` or `BufferError` explicitly.
- [LESSON] 2026-04-13: **DTCM Tensor Physical Bound Loopholes**: ANY unsplittable tensor chunk exceeding 12KB must immediately trigger an explicit `OutOfMemoryError`.
- [LESSON] 2026-04-24: **Exception Boundary Drift via Scope Refactoring**: The removal of inline exception imports (like `OutOfMemoryError`) poses a risk of masking organic boundary exceptions if not properly mapped to the top-level scope.
- [LESSON] 2026-04-13: **Fake Unit Test Fraud**: test_asm_gemm.py execution evasion via early returns and bypassing mathematical correctness validation is testing fraud.
- [LESSON] 2026-04-20: **Fake Unit Test Validation Fraud (Linker Bounds Illusion)**: Testing `.noinit` allocation by string-matching linker script content completely evades authentic physical limits evaluation. Tests MUST natively cross-compile the payload and explicitly parse the ELF segments (e.g. via `riscv64-unknown-elf-readelf -S`) to assert that the segments are mapped accurately (e.g., `>= 0x20000000`).
- [LESSON] 2026-04-13: **Fake Unit Test Validation Fraud via Trivial Math**: Replacing detonating stubs with trivial math operations (e.g., `Tensor.arange`) that completely lack physical boundary evaluations, cycle timing assertions, or RTL integration limits constitutes a test pyramid inversion and testing fraud. E2E tests must organically evaluate hardware execution boundaries.
- [LESSON] 2026-04-13: **GEMM Validation Sabotage**: Forcing early returns (`if DEV == "CORALNPU": return`) bypasses mathematical correctness validations and hardware FP register limits. It is forbidden.
- [LESSON] 2026-04-13: **Hardware Constraints**: Ensure the compiler natively raises `OutOfMemoryError` during code generation when evaluating `dtypes.count > 28` for vector upcast limits, correctly enforcing DTCM buffer constraints.
- [LESSON] 2026-04-13: **Hardware Constraints**: ML Compiler heuristics like `max_upcast` must be strictly physically bounded (e.g. `max_upcast = 28` for CORALNPU) based on architectural limits such as the DTCM memory equation. Relying on default framework heuristics (like 63 or 32) exceeds memory constraints and mathematically guarantees `OutOfMemoryError`.
- [LESSON] 2026-04-13: **Harness.yaml Execution Configuration**: Ensure `test_cmd` is flattened into a single line, prepended with `rm -f /dev/shm/coralnpu_* &&` to proactively clear IPC leaks, and logically split into Strict Baseline and Informational Prototype runs to prevent overarching watchdogs from deadlocking on Detonating Stubs.
- [LESSON] 2026-04-24: **Inline Import Shadowing**: Avoid inline local imports (like `import unittest`) in tests like `test_asm_gemm.py`. Move them to the global top-level scope to unmask boundary tests and prevent `UnboundLocalError`.
- [LESSON] 2026-04-23: **Inline Import Shadowing**: Avoid using inline local imports (e.g., `from tinygrad.codegen.opt.heuristic import OutOfMemoryError`) inside functions if the module is already imported globally, as this can cause `UnboundLocalError` shadowing during execution. Always rely on the global import.
- [LESSON] $(date -u +%Y-%m-%d): **Intra-Process Mocked IPC Teardown (Fake Unit Test)**: Evaluating IPC shared memory teardown by explicitly locking `memoryview` within the parent thread to artificially trigger a BufferError is testing fraud. Authentic cross-component integration requires spawning a worker process that holds the lock or crashes natively.
- [LESSON] $(date -u +%Y-%m-%d): **Intra-Process Mocked IPC Teardown**: Evaluating `BufferError` shared memory teardown faults by explicitly locking `memoryview(shm.buf)` inside the parent thread is a testing illusion. Spawn a `multiprocessing` worker process to hold the lock.
- [LESSON] 2026-04-23: **Intra-Process Mocked IPC Teardown**: Evaluating BufferError shared memory teardowns by locking memoryview(shm.buf) inside the parent thread actively masks genuine cross-process lock exhaustion limits. Tests must spawn authentic worker processes.
- [LESSON] $(date -u +%Y-%m-%d): **IPC Exception Masking & Atexit Handling**: When `_safe_release_ipc` raises `FileNotFoundError` immediately, it crashes the `__del__` loop that iterates over multiple shared memory segments, causing cascading memory leaks. Collect exceptions such as `FileNotFoundError` during release attempts and raise them at the end of the `__del__` block to guarantee full teardown of all allocated segments.
- [LESSON] 2026-04-12: **IPC Teardown Exception Fidelity**: When cleaning up multiprocessing shared memory (`shm.unlink()`), explicitly assert `except (ProcessLookupError, BufferError):` instead of blanketing exceptions or swallowing `FileNotFoundError`. Swallowing `FileNotFoundError` leaks shared memory into `/dev/shm` when the orchestrator watchdog violently crashes the pipeline, precipitating cascading `FileExistsError` deadlocks across future runs.
- [LESSON] $(date -u +%Y-%m-%d): **IPC Teardown Fidelity & Exception Returning:** When cleaning up multiprocessing shared memory natively inside `__del__`, helper functions like `_safe_release_ipc` MUST NOT forcefully `raise` errors on the first `FileNotFoundError` or `ProcessLookupError`, as this interrupts the garbage collector iterating over other active shared memory handles. Instead, these helpers must collect and `return` all encountered exceptions so that `__del__` can evaluate the entire block natively and raise them at the very end.
- [LESSON] $(date -u +%Y-%m-%d): **IPC Teardown Leakage Panic (Atexit Crash)**: Raising exceptions inside the `__del__` block violently crashes the garbage collector, preventing teardown of remaining shared memory segments. Teardown blocks must safely iterate and clear all segments before handling error accumulation logic.
- [LESSON] $(date -u +%Y-%m-%d): **Native Execution Deadlock Misattribution (`ops_cpu.py`)**: When massive tensors (e.g. 234MB) are generated using `Tensor.randn(device="CPU")`, the `prg.fxn()` stack trace in `ops_cpu.py` indicates the CPU backend thread pool is executing slow math operations (like `sin` and `log2`). This can take >30s on CI runners, triggering `pytest-timeout(30.0s)`. The orchestrator frequently misattributes this to a C++ hardware payload deadlock. To prevent overarching timeouts, tests verifying compiler OOM bounds (`allow_scale=True`) should use `Tensor.empty` to bypass the slow CPU generation when the payload is mathematically guaranteed to exceed DTCM limits.
- [LESSON] 2026-04-13: **Out-of-Scope Reversions**: Reverting out-of-scope cosmetic violations in apps, viz, and examples is a critical prerequisite to restoring test stability and preventing drive-by refactoring pollution.
- [LESSON] $(date -u +%Y-%m-%d): **Overarching Watchdog Deadlock Avoidance**: Do not rely on orchestrator watchdogs; isolate deadlocks natively (e.g. `pytest -x -n 0`) before concluding build mitigations.
- [LESSON] $(date -u +%Y-%m-%d): **Overarching Watchdog Deadlock Evasion**: Setting artificially low timeouts (e.g., `prog(timeout=0.001)`) to spoof `subprocess.TimeoutExpired` actively masks true native ISS watchdogs and allows deadlocks to persist.
- [LESSON] 2026-04-22: **Pytest Watchdog Deadlock Unmasking**: To resolve Pytest Watchdog Deadlocks where tests (like `test_asm_gemm.py`) hang and trigger the overarching 120s bash timeout, the test MUST be isolated in `harness.yaml` and executed natively with fail-fast arguments (e.g. `pytest -x -n 0 -v --timeout=30 --timeout-method=signal`). This allows the native timeout mechanism to catch the hang and generate an actionable traceback for the QA agent, preventing the entire pipeline from collapsing.
- [LESSON] 2026-04-22: **Pytest Watchdog Deadlock**: If test_asm_gemm.py or other assembly GEMM evaluations hang indefinitely, they will trigger the overarching SIGKILL. Isolate the test and run the suite natively with fail-fast arguments (pytest -x -n 0) to unmask the native deadlock.
- [LESSON] $(date -u +%Y-%m-%d): **QA Routing Pre-Requisite**: When Tier 1 blockers are resolved natively, QA must execute to unmask the pipeline before JIT Sync.
- [LESSON] $(date -u +%Y-%m-%d): **Real LLM E2E Validation**: Avoid using mocked models (`Mock`) in `test_llm_server.py`. Instantiate a real `Transformer` and a `SimpleTokenizer` with a complete byte mapping (up to vocab size) to natively evaluate the integration boundary between the server and the transformer execution loop. Ensure `max_context` is sufficiently large to prevent `ValueError: size mismatch` when passing multi-turn conversations.
- [LESSON] 2026-04-25: **Reversion to Skip Evasion Testing Fraud**: Reintroducing `@unittest.skipIf` into tests (like `test_dtype.py`) explicitly sabotages `try-except` boundary validation and constitutes testing fraud.
- [LESSON] 2026-04-14: **Target Compiler Constraints Strictness**: When running target checks like `is_cdna4()` in testing frameworks, always guard device resolution (e.g., `dev != "AMD"`) before accessing renderer architectures. This prevents explicitly targeting CoralNPU backends from throwing exceptions when attempting to evaluate irrelevant vendor architectures on generic simulators.
- [LESSON] $(date -u +%Y-%m-%d): **Testing Fraud via Execution Evasion**: Bypassing mathematical correctness bounds (`np.testing.assert_allclose`) in `test_asm_gemm.py` via early returns actively drops core algorithmic validation. E2E integration suites MUST evaluate the cross-compiled payloads against mathematical accuracy without early returns.
- [LESSON] 2026-04-24: **Testing Fraud via Skip Evasion**: Massive `@unittest.skip` chains in tests like `test_dtype.py` must be removed and refactored to organically evaluate structural hardware and data-type constraints natively, avoiding testing fraud.
- [LESSON] 2026-04-13: **Tinygrad Ping-Pong Chunking Math**: The complete DTCM tensor equation is explicitly `12KB (Ping) + 12KB (Pong) + 4KB (Output Accumulator) = 28KB Tensor DTCM`.
- [LESSON] 2026-04-25: **Watchdog Deadlock Evasion via Timeout Masking**: Applying a `@pytest.mark.timeout(30)` decorator to a failing test (e.g., `test_asm_gemm.py`) hides the true AXI stall. The underlying deadlock must be structurally resolved, not just hidden by a timeout.
- [LESSON] 2026-04-24: **Watchdog Deadlock Unmasking (`tinygrad`)**: Overarching watchdog deadlocks (e.g. 120s SIGKILLs) mask tracebacks and coverage. Failing targets (like `test_dtype.py`) MUST be isolated and run natively with fail-fast options (`pytest -x -n 0`) to organically expose the true hang.
- [LESSON] 2026-04-12: In `tinygrad` tests, when moving standard/third-party imports to the top level, ensure that PyBind11 bindings (e.g., `CoralNPUDevice`) remain strictly in the local scope of worker functions to prevent test runner crashes during multiprocessing.
- [LESSON] $(date -u +%Y-%m-%d): Python `ruff` linters strictly enforce 150-character line lengths and trailing whitespace limits. Code generators and test scripts (like `coralnpu.py` and `test_coralnpu_e2e.py`) must emit properly formatted strings and adhere to these boundaries.
- [LESSON] 2026-04-12: When extracting repetitive exception masking logic for resource cleanup (like IPC shared memory or locks), consolidate it into a dedicated helper function (e.g., `_safe_release_ipc`) to enforce uniform error logging and prevent accidental swallowing of critical ProcessLookupError or BufferError exceptions.
- [LESSON] $(date -u +%Y-%m-%d): When writing API compliance tests for secondary renderers (like `ClangJITRenderer`), always explicitly bind the evaluation tensors to `device="CPU"` or `device="CLANG"` to prevent `Device.DEFAULT` resolution from incorrectly invoking the NPU allocator pipeline during pure C++ AST validation.
