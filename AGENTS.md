# tinygrad Multi-Agent Constraints

### Execution & Architecture
* **.bss Obliteration & DTCM Allocation**: Main I/O buffers must be declared as file-scoped global arrays explicitly annotated with `__attribute__((section(".noinit")))` to prevent the `_start` phase from wiping host-streamed DMA weights.
* **Fake ELF Generation**: Do not utilize `create_dummy_elf` to manually write raw byte structures, bypassing authentic binary evaluation in the execution engine.
* **Full INT8 Quantization & Late Dequantization**: `Int8Linear` must perform native integer vector ops (`x_int8.dot(w_int8)`) with scaling applied strictly *after* the dot product to preserve memory footprints.
* **Ops.EXPAND Shape Requirement**: Ensure `Ops.EXPAND` operations correctly handle shape expansion mismatches to avoid unhandled `RuntimeError` and `ValueError` crashes.
* **Ops.EXPAND Symbolic Evaluation**: Use the `resolve()` helper to safely evaluate symbolic ranges (UOp) within `Ops.EXPAND` boolean matching, preventing `ValueError` crashes on unresolved vars.
* **Organic Hardware Traps**: Do NOT use Python regex assertions (`assertRaisesRegex`) to bypass memory bounds traps. The simulator MUST dynamically compile and bounds-trap the generated `.elf` payload.
* **Python Line Length & Timeout Obfuscation**: Enforce strict 80-character line lengths to prevent the obfuscation of arbitrary hardware execution bounds (e.g., `timeout=15.0`) within massive one-liners.
* **Register Bounds Limits vs DTCM**: Explicitly cap unroll sizes at `max_upcast = 28` for the `CORALNPU` target to preserve at least 4KB of DTCM for C-stack overhead, preventing stack overflows.

### IPC & Test Runner Deadlocks
* **IPC Isolation and Worker Lifecycle**: Tests such as `TestIpcWorkerPool` MUST evaluate true multiprocess lockstep execution using `multiprocessing.shared_memory`. Overarching OS signal injections are banned; worker execution timeout limits MUST trigger native graceful `TimeoutError` exceptions.
* **IPC Teardown Reversion**: Monkeypatching `pytest_sessionfinish` and `multiprocessing.shared_memory` to forcefully terminate worker groups causes premature OS-level deadlocks (e.g., `OSError: cannot send (already closed?)`) during `pytest-xdist` execution. Authentic Python IPC lifecycles must be respected without unauthorized `atexit` OS-level `SIGKILL` interventions. Do not blindly execute worker teardown monkeypatches unless explicitly authorized by SPECS.md.
* **Multiprocessing Spawn Method**: Enforcing `multiprocessing.set_start_method("spawn")` globally at module scope prevents `pytest-xdist` fork deadlocks and gracefully supports native IPC teardown.
* **Multiprocessing Teardown**: `pytest-xdist` workers crashing mid-suite indicate improper state or IPC teardown within the ML framework tests.
* **Suite Unmasking Audit**: Resolving the OSError worker crash mandates an immediate QA audit to evaluate the newly revealed test assertions.

### QA & Refactoring Constraints
* **Collection Unmasking Audit**: Resolving pytest collection failures (e.g., missing imports or API drift) will unmask massive underlying functional failures. A full QA triage is mandatory immediately after unblocking `pytest`.
* **Cross-Compiler Testing Bound**: `tinygrad` tests (like `TestCoralNPURenderer`) MUST explicitly use the host `riscv64-unknown-elf-gcc` toolchain and Clang python bindings to parse the generated C++ AST, ensuring asynchronous DMA macros (`CORAL_DMA_ASYNC`) precede barriers.
* **Decoupled API Boundaries & Missing Standard Imports**: Cross-component refactoring must be strictly atomic. Modifying bindings or decoupling core modules (like `CompilerSet` from `tinygrad.device`) without updating all downstream callers causes catastrophic test collection failures. Always ensure standard library imports (e.g., `unittest`) are explicitly declared in test files. Prevent API drift causing massive test failures; maintain strict compiler boundaries. Ensure code correctly interacts with `dtypes.weakint` and `dtypes.index` changes without throwing unhandled exceptions.
* **Exception Swallowing Prohibition**: Catching integration/compilation failures via generic `except (FileNotFoundError, subprocess.CalledProcessError)` and routing to `unittest.SkipTest` mathematically erases test failures and is explicitly forbidden. Float8 compilation errors (`float8_e4m3`, `float8_e5m2fnuz`) must be allowed to fail organically or skipped properly based on hardware capability, not exception swallowing. Cosmetic refactors that mask organic test failures with `SkipTest` must be forcefully reverted. Modifying an upstream test to skip execution subverts the pipeline. Do NOT catch `subprocess.CalledProcessError` or `FileNotFoundError` using `@unittest.skip`. Mocking compilation borders mathematically erases integration boundary failure.
* **NV Interface Global Swallowing**: Instantiating objects like `Tensor(dat, device="NV")` wrapped in a blanket `except Exception: raise SkipTest` silences real architectural crashes (AttributeError, TypeError) and is strictly forbidden.
* **Recursion Limit Masking**: Wrapping deep graph evaluations in `try...except RecursionError: raise SkipTest` instead of fixing AST bounds is mathematically erasing test failures and is prohibited.
* **Refactoring Void Trapping**: Aggressively removing imports like `run_schedule` and `unittest` without verifying downstream test file dependencies leads to catastrophic collection failures (100% test void). All refactoring must execute a full `pytest --collect-only` before committing.

## Lessons Learned & QA Mandates
* **Configuration Obsolescence**: Tests failing due to `CPU=1` deprecation mask authentic architecture faults. Ensure `DEV=CPU` and current API targets are strictly preserved in orchestration configs to avoid 100% test failure loops.
* **Cross-Compiler Enforcement**: Cross-compiler tests implemented in `test_cross_compiler.py` must organically fail if the `riscv64-unknown-elf-gcc` compiler is absent. Catching `FileNotFoundError` to skip tests mathematically erases the CI failure.
* **IPC Worker Mocking Ban**: Parallel execution state MUST NOT be mocked with purely Python-based dummy workers. Tests must schedule authentic cross-compiled compute kernels to accurately measure shared memory teardown boundaries natively.
* **Mocked Fetching Prevention**: Do not isolate logic by instantiating `MockResponse(img_bytes)` via `@patch('urllib.request.urlopen')` to fake image fetching. Authentic E2E validations proving cross-component fetch viability are required.
* **Native Bound Assertions**: Tinygrad tests (e.g., DMA boundary checks) enforce physical limits (like the 4KB chunk boundary) by compiling minimal C/C++ stubs with native `abort()` traps on the host. This prevents "testing illusions" by ensuring boundary checks happen natively rather than relying on Python regex or string-matching of ASTs.
* **Rangeify Double Bufferize Deadlocks**: When folding perfectly matching `.contiguous()` views that cancel each other out, `run_rangeify` natively emits stacked `BUFFERIZE` nodes with `removable=False`. To prevent the simplifier from locking into a redundant memory realization, `pm_remove_bufferize` MUST include a `fold_double_contig` pattern that actively substitutes ranges and replaces the `INDEX` chain, circumventing the rigid `removable=False` checks designed for standard variables.
* **Upstream Synchronization Priority:**: Upstream changes (e.g. CUDARenderer refactoring) can break local dependencies. JIT Upstream sync tasks must be scheduled atomically per-submodule when divergent.

## IPC Worker E2E Mocking Illusion
Exercising Python SharedMemory primitives with mocked tasks (e.g., numpy array doubling) fails to prove NPU execution. Tests must schedule authentic cross-compiled compute kernels to accurately measure shared memory teardown boundaries natively.

## Global Evasion via Toolchain Bypassing
Aggressively catching missing cross-compiler errors (like `FileNotFoundError`) to skip tests mathematically erases architectural test failures and evades E2E constraints.

## Compiler & Execution Bounds
* **Beam Search False-Positive Evasion**: The Python simulation wrapper MUST evaluate semantic correctness BEFORE accepting the `mcycle` cost to prevent poisoning the optimizer with zero-cost early exits.
* **Pytest Fatal Watchdog Termination & Debugging**: When the CI pipeline is brutally terminated by a global timeout watchdog mid-execution (e.g., 120s limit), `pytest` will not emit its standard failure summary or stack traces. To diagnose explicit test failures (`F`) that occur before the timeout, developers MUST run the suite with fail-fast enabled (`pytest -x`) or disable parallel workers (`-n 0`). `pytest -n auto` is strictly forbidden when overarching watchdogs are present as it obliterates stack traces on timeout.
* **Unaligned DMA Evasion**: The ML compiler MUST strictly assert that all `GLOBAL` memory pointers passed to DMA copy intrinsics are aligned to the exact AXI bus width prior to compilation to prevent bus faults.

### Execution Evasion Remediation (Lesson)
* **Authentic ELF Generation Constraints:**: When testing `CoralNPURenderer` outputs, never mock ELF structures via Python byte arrays. Use `subprocess` pointing to `riscv64-unknown-elf-gcc -nostdlib` to organically verify `.bss`, `.noinit`, and `.text` section bounds, ensuring that missing cross-compilers cause the pipeline to organically fail.
[FLAG: stale] * **IPC Worker E2E Mocking Illusion**: Exercising Python SharedMemory primitives with mocked tasks (e.g., numpy array doubling) fails to prove NPU execution. Tests must schedule authentic cross-compiled compute kernels to accurately measure shared memory teardown boundaries natively.
* **Missing Timeout Enforcement**: If the execution engine uses unbounded blocking waits (e.g., `p.wait()`), overarching Python watchdogs are fake and will deadlock on native C++ simulator hangs.
* **Python Worker Mocking Illusion**: Pure Python `time.sleep` loops masquerading as E2E IPC deadlock/timeout tests are invalid. Authentic cross-compiled compute kernels must be scheduled on the simulator to prove C++ process teardown.
* **VMM Boundary Mocking**: Generating fake C files to extract symbols (like `_end`) bypasses the actual firmware subsystem linker and mocks the integration boundary for memory map setup. Authentic firmware must be natively evaluated.

### Python Multiprocessing & Shared Memory GC
[FLAG: invalid] * **Shared Memory Exhaustion:**: Overarching CI timeouts or crashes leave orphaned SharedMemory blocks in `/dev/shm`, causing physical memory exhaustion across subsequent test runs. Always implement a `pytest_sessionfinish` hook in `conftest.py` to aggressively garbage collect `/dev/shm` blocks upon suite termination.
* **Watchdog Bounding (Unbounded `p.wait()`):**: Do not enforce overarching test timeouts using unbounded blocking waits (e.g., `subprocess.Popen.wait()`) on simulator subprocesses. If the simulator hangs (e.g., AXI bus lock), the wait will deadlock the entire CI pipeline. Use native PyBind11 timeouts instead.

# Tinygrad Module Testing Directives
* **Cosmetic Refactoring Hiding Lack of Validation**: Fixing PEP8/Lint violations (e.g., using `ruff` for F401, F811) provides clean test logs but fails to increase any underlying logic or execution bounds coverage. Cosmetic unblocking must not be confused with authentic execution testing.
[FLAG: stale] * **Coverage Collapse due to Global Watchdogs**: A drastic dip in code coverage coupled with 100% unit test pass rates signifies a total suite deadlock. Overarching watchdogs (SIGKILL) on `pytest-xdist` runners erase stack traces and coverage artifacts. You MUST run `pytest -x -n 0` to organically catch the unmasked failures natively.
[FLAG: stale] * **Cross-Compilation Linker Evasion**: Using `-nostdlib` during cross-compilation tests (e.g., for E2E integration) WITHOUT explicitly invoking the authentic NPU linker script (`-T linker.ld`) completely bypasses DTCM and EXTMEM physical boundaries. All hardware boundary integration tests MUST invoke the correct linker script.
* **DTCM DMA Chunk Limit Validation:** Ping-Pong DMA memory transfers are strictly limited to 4KB chunks. If chunks exceed 4KB, the compiler must organically throw an `OutOfMemoryError` instead of falling back to host `.bss`.
* **E2E IPC Enforcement**: Never mock parallel NPU execution with Python `time.sleep` loops. E2E tests must schedule authentic cross-compiled compute kernels on the NPU simulator.
* **ELF Patcher Teardown Ordering**: When tests patch `CORALNPU_ELF` in `os.environ` (e.g., via `unittest.mock.patch.dict`), the `patcher.stop()` MUST be called *after* any cleanup logic that instantiates `CoralNPUDevice`. Instantiating the device after stopping the patcher will cause the allocator to search for a non-existent `coralnpu.elf` and crash with `RuntimeError: VMM base address (_end) not found`.
* **Firmware Bypassing via Empty ELFs**: When testing `CoralNPUDevice` or `CoralNPUAllocator` initialization, do not generate a dummy `.c` stub with `void _start() { asm volatile("ebreak"); }` or an empty `.s` file. Authentic NPU firmware payloads must be cross-compiled containing `.bss`, `.data`, and `.noinit` sections with a correctly aligned `__stack_end__` variable (not `_stack_top`) to evaluate physical linker constraints natively.
* **Framework Boundaries**: `tinygrad` is purely a Python ML framework. Do not instruct agents to implement native C++ functionalities (like `SimTimeoutError`) within the `tinygrad` submodule; these belong in C++ simulation repositories like `coralnpu-mpact`.
* **Hardware Simulator Evasion via Decorators**: Masking critical IPC boundaries behind `@unittest.skipIf` decorators evades authentic cross-compiled tests. If the simulator is missing, tests report 100% success while the execution boundary remains unexercised. This is testing fraud.
[FLAG: stale] * **Harness Execution Consistency (Env Eval):** When setting environment variables like `LIBCLANG_PATH` at the beginning of `test_cmd` in `harness.yaml`, the command string MUST be prefixed with `env ` (e.g., `env LIBCLANG_PATH=... python3 -m pytest`). Failing to prefix with `env` causes the Bash executor to incorrectly interpret the entire string as a single missing executable path, resulting in a fatal `No such file or directory` error that entirely blanks test execution.
[FLAG: stale] * **Harness Execution Consistency**: Ensure the `test_cmd` within `harness.yaml` reflects the `-x -n 0` requirement when debugging overarching watchdog timeouts to prevent CI pipeline deadlocks and stack trace erasure.
* **Linker Template Variables & Stack Assertions**: When manually instantiating `coralnpu_tcm.ld.tpl` for test payloads, ensure `@@STACK_START_SPEC@@` is replaced with `. = ORIGIN(DTCM) + LENGTH(DTCM) - STACK_SIZE;` rather than an empty string to satisfy the 28K C-Stack linker safety assertion (`__stack_start - __bss_end >= 28672`). Additionally, the top of the stack is identified natively by `__stack_end__`, not `_stack_top`.
* **Merge Conflicts and Stashing**: When pulling upstream commits using `git merge --no-ff`, temporarily `git stash` any unstaged changes to prevent merge aborts, and pop them after resolving conflicts.
* **Missing Explicit Imports**: Unresolved names (e.g., `F821 Undefined name 'Tensor'`) create 100% test voids. You MUST inject explicit imports (like `from tinygrad.tensor import Tensor`, `import numpy as np`) to restore valid execution pathways.
* **Ping-Pong Chunk Capacity Limits:** When allocating double-buffering ping-pong DMA chunks in the DTCM, the compiler MUST enforce a strict `CHUNK_LIMIT = 4096` bytes per chunk as mapped in DESIGN.md. Exceeding 4KB per chunk will instantly overflow the physical boundaries and guarantee OOM traps.
[FLAG: stale] * **Planner Hallucination regarding Regex**: The planner may hallucinate the presence of regex like `--instrumentation_filter` in `harness.yaml` for submodules where it does not exist (e.g., `tinygrad`). Agents must verify the file contents before attempting to fix regex escapes.
* **Subsystem Mock Banning**: Never mock memory map initializers by compiling empty ELFs to extract an `_end` symbol.
* **Testing Fraud via Overarching Watchdogs**: Do not implement `p.wait(timeout)` and silently catch `subprocess.TimeoutExpired`. The overarching Python watchdog actively masks native simulator hangs. Native execution must halt organically and trigger `absl::DeadlineExceededError`.
* **Type Hint Refactoring & NameErrors**: When refactoring Python files to resolve linting errors (like unused imports), do not over-strip types from the `typing` module (e.g., `Any`, `List`, `Dict`, `Tuple`) that are actively used in variable annotations. Removing these introduces explicit `NameError` crashes at runtime and breaks static analysis.
* **Upstream Synchronization Testing Paths**: `test_ops.py` is located at `test/backend/test_ops.py`, not in the root `test/` directory. Be careful with test paths.
* **Watchdog Boundaries**: The python-level timeout watchdog tests must prove cross-boundary simulation termination (e.g., verifying `SimTimeoutError` from C++), not just block on `p.wait()`.
* **Watchdog Bypassing via Cosmetic Fraud**: Leaving code comments `# Tested via pytest` or narrowing Python `except` blocks (like `ProcessLookupError`) without instantiating genuine cross-compiled C++ IPC workers is testing fraud and fails to organically test the overarching thread/process teardown lockstep natively.
- Code Coverage Integrity: A 100% pass rate on drastically reduced execution coverage (e.g., 21.85%) due to SIGKILL timeout deadlocks is a mathematical fraud. Native C++ simulator hangs MUST be organically trapped natively to prevent blindfolding the CI pipeline.

# QA & Ledger Execution Mandates
[FLAG: stale] *   **Data-Loss Audit Requirement**: Always perform a data-loss audit before ending the execution cycle.
[FLAG: stale] *   **No Testing Illusions**: Do not mock physical TileLink/AXI boundaries with dictionaries. Instantiate genuine SystemVerilog responders.
[FLAG: stale] *   **Test Runner Watchdogs**: Always use `pytest -x -n 0` to prevent watchdogs from terminating the execution silently.
* **Coverage Voids via Ignored Integration**: Achieving only 22% coverage despite overarching E2E unmasking points to inverted Test Pyramids and missed unit tests in backend renderers.
[FLAG: stale] * **E2E Execution Unmasking**: Explicitly append authentic execution scripts like test_coralnpu_e2e.py to the test_cmd string to prevent pipeline coverage voids.
[FLAG: stale] * **Exception Masking Eradication (tinygrad)**: Removed `self.skipTest()` masking for `ProcessLookupError` during xdist teardown. Masking such exceptions hides genuine IPC failures and root structural crashes (like missing `_end` symbols in ELFs). Native unmasked execution is required to expose these blockers.
[FLAG: stale] * **Test Pyramid Inversion**: Unmasking overarching E2E execution tests without establishing underlying functional unit/integration checks for local components (such as renderers and memory allocators) will result in stagnant code coverage and brittle architectures.
[FLAG: stale] * **YAML Quoting Integrity**: When modifying string values like `test_cmd` in `harness.yaml` via sed, ensure the appended test paths are placed strictly *inside* the closing quote (e.g., `s|test.py"|test.py new_test.py"|`). Appending at the end of the line breaks YAML syntax by placing arguments outside the quotes.
[FLAG: stale] * **Test Runner Deadlock Masking**: Relying on the 120s orchestrator watchdog to terminate `test_multitensor.py` masks authentic deadlocks. Use `pytest -x -n 0` to unmask failures organically.
* **Orphaned Integration Illusion (harness.yaml)**: Newly developed E2E integration suites and microbenchmarks (like `test_ping_pong_address_overlap.py`) must be explicitly appended to the `test_cmd` execution string inside `harness.yaml`. Neglecting to add the test file to the harness completely isolates it from the CI pipeline, resulting in a false 100% pass rate that mathematically ignores the new boundary constraint.
* **Cross-Compilation Name Mangling Constraint (tinygrad)**: When natively evaluating physical NPU memory boundaries with `riscv64-unknown-elf-gcc -T linker.ld`, avoid generating `constexpr` features and compiling with `g++`. C++ name mangling breaks the bare-metal `_start` assembly initialization block (`call test`). The ML Compiler payload generator MUST be optimized for pure C compatibility (`const int`) so that `gcc` successfully evaluates the artifact without `undefined reference` linkage failures.
* **Duplicate Hardware Handoffs (`_start`)**: `CoralNPURenderer.render_kernel` organically injects the `_start()` hardware handoff logic at the end of the UOp payload. Injecting a secondary manual `_start` function inside evaluation tests leads to immediate `redefinition of _start` linkage crashes.

* **Missing Simulator Bindings Masking .noinit Alignment:** The FileNotFoundError masking of `coralnpu_v2_sim` in E2E tests hid a critical divergence between the allocator's simulated SHM base address (`buf_handle`) and the statically linked `.noinit` execution arrays mapped by the ML compiler. The simulator MUST dynamically parse the cross-compiled `.elf` symbol table (extracting the physical addresses of `data0`, `data1`, etc.) to map the IPC `shm` pointers organically. Relying on `buf_handle` directly fails hardware bounds.
* **Orphaned Test Void Diagnosis**: When tests for cross-compilation boundaries or IPC teardown deadlocks are added (e.g. `test_pytest_xdist_teardown_integration.py` and `test_cross_compiler_matrix.py`), explicitly ensure they are present in `test_cmd` of `harness.yaml`. Otherwise they silently evaluate to a 100% test pass while the test suite suffers from a structural coverage void.

* **Shared Memory Teardown Leakage**: Abruptly killed IPC runners (e.g., via 120s watchdogs) leak shared memory segments into `/dev/shm`, causing catastrophic `FileExistsError` and `ProcessLookupError` deadlocks on all subsequent test suite runs. Always prepend `rm -f /dev/shm/*` to the overarching test execution command in `harness.yaml` to ensure a pristine IPC environment before running Pytest.

### Execution Exception Boundaries
* **Exception Masking Eradication**: Blanket `except Exception: pass` actively violates architectural IPC teardown loops by swallowing `FileNotFoundError`, `ProcessLookupError`, and `BufferError`. These must be explicitly defined to allow teardown fidelity.
* **Per-Test Watchdog Isolation**: Using `@pytest.mark.timeout(30)` isolates tests organically and unmasks hangs (e.g. `test_asm_gemm.py`), preventing the overarching CI watchdog from brutally killing the runner and erasing stack traces.
* **Exception Masking Eradication**: Blanket `except Exception: pass` actively violates architectural IPC teardown loops by swallowing `FileNotFoundError`, `ProcessLookupError`, and `BufferError`. These must be explicitly defined to allow teardown fidelity.
* **Per-Test Watchdog Isolation**: Using `@pytest.mark.timeout(30)` isolates tests organically and unmasks hangs (e.g. `test_asm_gemm.py`), preventing the overarching CI watchdog from brutally killing the runner and erasing stack traces.
* **Test Pyramid Inversion (AST Transformation)**: Relying exclusively on E2E compilation constraints to catch rendering bugs (like ping-pong tiling logic) represents a dangerous Test Pyramid inversion. All compiler UOp/AST transformation passes must have dedicated local unit tests.
* **Shared Memory Teardown Leakage**: Abruptly killed IPC runners (e.g., via 120s orchestrator watchdogs) leak shared memory segments into `/dev/shm`, causing catastrophic `FileExistsError` deadlocks on all subsequent pipeline runs. You must ensure rigorous teardown via `rm -f /dev/shm/*` before test executions or strictly enforce `atexit` bounds natively.
* **Path Verification Mandate**: When appending orphaned tests to harness.yaml, always verify the exact path using find. Do not blindly append hallucinated directory paths (e.g., test/test_models/ instead of test/models/) to test_cmd, as this causes the test framework to crash with 'No such file or directory'.
* **Test Pyramid Inversion (AST Transformation)**: When adding or refactoring AST transformations (like ping-pong tiling) inside the renderer, explicit local unit tests must be added to ensure proper isolated validation. Relying entirely on E2E integration failures for functional logic inversions the testing pyramid.
\n* **Fake Unit Test Fraud**: Tests that contain an unconditional `pass` block or weak type assertions evade actual logic validation under the guise of coverage. AST transformations and similar logic must enforce exact physical offsets and bounds, without unconditional pass blocks.\n* **E2E Hardware Mocking Illusion**: Validating ping-pong tiling or other hardware logic by only verifying compiler exit codes statically is testing fraud. A true E2E execution over the hardware simulator (e.g. `coralnpu_v2_sim`) must be implemented to test physical boundaries.\n* **Watchdog Deadlock Unmasking**: Test suites deadlocking on JIT evaluations and hitting overarching CI timeouts (e.g., 300s) will have their native stack traces erased due to SIGKILL on multiprocessing teardown. You must isolate and run the suite natively with fail-fast execution (`pytest -x -n 0`) to expose the true hang.
* **SkipTest Illusion**: Using skipTest for missing hardware simulator artifacts completely masks the E2E boundary constraint and falsely inflates pass rates. Tests must organically crash with FileNotFoundError when artifacts are missing.
* **Exception Swallowing Prohibition**: Python code MUST NEVER use a blanket `except Exception: pass`, as this actively masks `FileNotFoundError`, `ProcessLookupError`, and `BufferError`, burying IPC teardown deadlocks and crashing traces.
* **Black Box Opaque Logic Restriction**: Utilizing opaque bit-slicing (e.g., masking upper 64 bits of `UOpAstHash`) without explicit documentation outlining the mathematical heuristics and Protobuf layout boundaries is highly unorthodox and dangerous.
* **Mocked Exception Boundary Illusion**: Testing Python exception teardowns (like BufferError in SharedMemory) requires actively locking resources (e.g., memoryview(shm.buf)) before cleanup. Merely triggering native FileNotFoundError while claiming to evaluate BufferError evades the boundary entirely.
* **BufferError Mocking Mitigation**: To authentically trigger a `BufferError` during shared memory teardown in tests, an active `memoryview(shm.buf)` lock must be established and held while `shm.close()` is executed. Calling `shm.unlink()` without an active memoryview only natively forces a `FileNotFoundError`, representing a testing illusion.
* **Fake Unit Test Prevention**: Tinygrad UOp compiler tests must explicitly assert mathematical constraints like DTCM bounds (`dtcm_usage <= 28 * 1024`) rather than relying on weak type assertions or unconditional `pass` blocks.
* **IPC Lock Exhaustion**: `pytest-xdist` concurrency can mask underlying teardown deadlocks in tests like `test_multitensor.py`. Framework watchdogs killing test runners wipe stack traces, requiring explicit isolated execution to unmask.
* **Local Logic Dip (Exception Masking)**: Exception handling blocks for IPC (e.g., BufferError) lacking comprehensive native assertions and using blanket masks (like `except (FileNotFoundError, ProcessLookupError, BufferError): pass`) evade native hardware memory tracking validation and constitute a testing illusion.
* **Ping-Pong Buffer Sizes**: Enforce Ping and Pong chunk sizes to 12KB (12288 bytes) each within the coralnpu renderer to correctly map the 28KB Tensor DTCM limits without overlapping into the 4KB C-Stack/BSS.
* **IPC Lock Exhaustion & Overarching Timeout**: `pytest-xdist` concurrency can mask underlying teardown deadlocks. Frame watchdogs (e.g., 30s) brutally kill test runners. These deadlocks must be organically trapped natively instead of relying on external watchdogs.
* **Local Logic Dip Causing Test Voids**: Removing exception types (like `BufferError`) from teardown blocks without native wrapper assertions causes fatal pipeline collapses due to unhandled exceptions.
* **Exception Masking Illusion (BufferError)**: Adding specific teardown exceptions like `BufferError` to a blanket exception masking block (e.g., `except (... BufferError): pass`) hides IPC teardown failures. You must use native test assertions to validate teardown boundaries.
* **Unverified Ping-Pong Constraints**: When modifying Ping-Pong buffer capacities (e.g., from 4KB to 12KB), you MUST simultaneously introduce explicit unit tests verifying the new mathematical boundaries (e.g. `dtcm_usage <= 28 * 1024`). Changing constraints without associated boundary tests creates a critical coverage gap.
# Tinygrad Submodule Guidelines
* **Exception Masking Illusion**: Adding `BufferError` or `ProcessLookupError` to a blanket exception masking block (e.g., `except (..., BufferError): pass`) hides IPC teardown failures. You must use native test assertions to validate teardown boundaries.
* **DTCM Chunk Verification**: Tiling equations must allocate 12KB Ping + 12KB Pong + 4KB Output Accumulator + 4KB C-Stack/BSS = 32KB total physical layout. Ensure unit tests explicitly assert these mathematical boundaries (e.g., `dtcm_usage <= 28 * 1024`).

* **IPC Teardown Assertions**: When fixing exception masking around IPC teardown, explicitly assert `ProcessLookupError` and `BufferError` instead of using blanket `except` statements, and organically acquire locks (`memoryview`) to natively validate lock exhaustion.
* **IPC Lock Exhaustion Validation**: To prevent teardown exception masking in integration suites, test runners must explicitly acquire a locked `memoryview(shm.buf)` to empirically validate `BufferError` limits during teardown sequences, rather than relying on blanket `except (BufferError, ProcessLookupError):` clauses which hide silent pipeline leaks.
* **Exception Masking Test Crash**: In `test_pytest_xdist_teardown_integration.py`, the test `test_buffer_error_consumed_safely` establishes a `memoryview` lock to organically trigger a `BufferError`. However, because the underlying allocator (`ops_coralnpu.py:_free()`) correctly catches it and re-raises it as `AssertionError("IPC Lock Exhaustion: ...")`, the test runner crashes natively without catching the exception natively via `self.assertRaises(AssertionError)`. Tests must use `self.assertRaises(AssertionError)` to prevent a fatal pipeline collapse.
* **AssertionError Wrapping Strictness**: When an IPC memory teardown failure (e.g. `BufferError` due to lock exhaustion) is properly trapped by the allocator and re-raised as an `AssertionError`, integration tests enforcing this teardown MUST cleanly catch the `AssertionError` via `self.assertRaises` to prevent fatal overarching orchestrator test runner crashes.

* **Fake Unit Test Fraud**: Tests containing unconditional `pass` blocks, weak assertions, or asserting against dummy dictionaries actively evade logic validation. AST transformations and similar logic must enforce exact physical offsets and bounds natively.
* **Orphaned Integration Illusion**: Hacking test outputs to inject buffers instead of organically generating them decouples physical hardware bounds from compiler logic. Integration tests must natively execute organically produced payloads.
* **Overarching Watchdog Deadlock Masking**: Test runners hanging indefinitely and terminated by bash timeouts mask underlying deadlocks. The orchestrator must enforce fail-fast isolation (`pytest -x -n 0`) and native test isolation boundaries (e.g. `@pytest.mark.timeout(30)`) to natively unmask hangs.
* **Exception Masking Teardown Visibility**: Swallowing `FileNotFoundError` during IPC lock exhaustion limits mathematically erases genuine teardown failure visibility and allows overarching deadlock conditions to persist. Teardown blocks must explicitly assert lock exhaustion limits rather than using blanket exception masks.

* **String-Appending Compiler Evasion Fraud**: Drop the string-appending compiler evasion in tests. If a compiler pass (e.g., `ping_pong_tile`) modifies memory layouts, the Integration Test MUST natively execute the organically produced payload generated by the pass. Hacking or appending to the output strings decouples physical hardware bounds from compiler logic and constitutes testing fraud.
